# Companion Runtime 本地实施与验收

基线：main / df3a2fb，68 项测试通过。只修改本地分支 companion-runtime，审计前不上传。

1. 保留 SQLite records 和 V1 角色/记忆契约，以独立、版本化的 companion collection 保存新状态。Self Model 是查询视图，不再复制角色资料。
2. 新增有界 Companion 状态、Provider 数据和同一个主动联系决策入口。默认关闭主动联系；调度器只产生意图，宿主负责生成与发送。采用事务内保留与发送回执避免多调度器重复发送。
3. 接入 runtime_context、turn_commit、MCP；离线模拟只从低风险活动白名单选择，模拟来源不得提升为真实用户事实。角色导入导出兼容 V1，新增信息使用明确版本，默认剔除私人活动资料。
4. 精简为一个主 Skill 和按需 references；修复三种首次使用状态。使用原始 P1 资源和官方 manifest 字段。README 仅两条安装路线。
5. 检查旧分发和演示文件的消费者，先迁移必需测试辅助代码，再删除无用入口。保留 install.py 生命周期。
6. 运行单测、类型/格式/静态检查、构建、隔离安装生命周期和双 SDK 客户端共享后端验收。新增三系统 CI，但本轮不触发远端运行。
7. 输出逐项审计、删除理由、真实宿主/平台未验证项和 Git 状态；等待用户批准。

约束：不调用第二个 LLM，不发送真实主动消息，不改变用户真实宿主配置，不选 License，不上传任何数据。
