# 媒体与视觉身份

图像观察先存 MEDIA_DERIVED RawEvent，再 perception_observe。自动原型只有 candidate；用户明确“这是你”后在 OOC 读取 allowlist，携 USER_DIRECT 确认证据 confirm。SELF 识别需指向 trusted prototype。认出别的视觉对象不等于另一个 Runtime Character，禁止读取其私人记忆或自动启动它。

海边图片只能说明媒体里有什么，不能建立“今天去过海边”的现实经历。不要上传不必要原图或凭据；source_ref 只是用户可控引用，Core 不自动下载远程地址。
