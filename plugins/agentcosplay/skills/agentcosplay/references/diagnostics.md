# 诊断

runtime_doctor(session_id) 返回当前角色的记录计数、无效证据、过期任务、编译摘要及待确认投递统计。context_explain 返回 slot、预算与指纹。不要把私人 Memory、认证信息或数据库发到公共渠道。

安装和恢复使用仓库原生 doctor/installation smoke/migration check/runtime acceptance；合成人物仅临时库。未知投递先核查，不重发；坏数据先备份，不以删库掩盖问题。
