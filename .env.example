# Example names only. Never commit a real .env, token, or memory database.
# Default: OS user-data directory, outside the repository.
# CHARACTER_DATA_DIR=/absolute/path/to/private-character-data
CHARACTER_OWNER=local-user
CHARACTER_HOST=127.0.0.1
CHARACTER_PORT=8765
CHARACTER_TOKEN=


# Remote deployment is NOT configured. Set these only after explicit approval.
# CHARACTER_OAUTH_ISSUER=https://your-identity-provider.example
# CHARACTER_OAUTH_AUDIENCE=https://your-authorized-service.example/mcp
# CHARACTER_OAUTH_JWKS_URL=https://your-identity-provider.example/.well-known/jwks.json
# CHARACTER_RESOURCE_URL=https://your-authorized-service.example/mcp
